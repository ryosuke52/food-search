//const proxy = require('http-proxy-middleware');

//module.exports = function(app) {
 // app.use(proxy("/api/auth", { target: "http://localhost:8080/" }));
 // app.use(proxy("/api/users", { target: "http://localhost:8081/" }));
//};


//import proxy from 'http-proxy-middleware';
 
//module.exports = function(app) {
  //  const headers  = {
    //    "Content-Type": "application/json",
  //  }
   // app.use(proxy("{params}", { target: "http://webservice.recruit.co.jp/hotpepper/gourmet/v1/",changeOrigin: true,secure: false,headers: headers}));  
//};